[project]
name = "showcase-ai-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "showcase-shared",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.49.0",
    "redis>=5.2.0",
    "pydantic>=2.0",
]

[tool.uv.sources]
showcase-shared = { path = "../shared" }
