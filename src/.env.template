# Global environment template for all runnable services in `src/`.
# Copy this file to `.env` and adjust shared defaults.
# Service-specific templates are under `src/envs/<service>/.env.template`.

# Environment
PROJECT_NAME=showcase_project
APP_ENV=development

# Service Ports
CORE_PLATFORM_PORT=8001
AI_SERVICE_PORT=8002
OPS_API_PORT=8003
OPS_FRONTEND_PORT=3000

# Optional shared settings
LOG_LEVEL=INFO
