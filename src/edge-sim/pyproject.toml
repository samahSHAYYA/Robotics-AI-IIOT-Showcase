[project]
name = "edge-sim"
version = "0.1.0"
description = "Edge Device Simulator — simulated IoT sensor grid for the factory floor"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "redis>=5.2.0",
    "pydantic>=2.0",
]
