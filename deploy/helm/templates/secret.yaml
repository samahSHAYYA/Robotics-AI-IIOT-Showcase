apiVersion: v1
kind: Secret
metadata:
  name: {{ include "smart-factory.fullname" . }}-postgres-secret
  labels:
    {{- include "smart-factory.labels" . | nindent 4 }}
type: Opaque
data:
  POSTGRES_PASSWORD: {{ .Values.postgres.password | b64enc | quote }}
